# dnsmasq DHCP/PXE/split DNS

DHCP requires subnet/range/router/DNS options and single-authority on the L2 segment. PXE/TFTP requires boot options and file availability. Split DNS uses server/domain rules and must avoid loops and bogus upstream responses.
