# BIND zones, transfers, DNSSEC, views

Track SOA serials, primary/secondary roles, NOTIFY, AXFR/IXFR, TSIG, `allow-transfer`, and views. Split DNS requires validating each view from matching source networks. DNSSEC changes need DS/key rollover planning and rollback.
