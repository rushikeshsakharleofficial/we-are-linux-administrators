# named-expert method

Use evidence first: config syntax, zone validity, role, recursion exposure, transfer ACL, logs, and `dig` from relevant networks. Validate with `named-checkconf -z`, `named-checkzone`, `rndc status`, and external queries before reload.
