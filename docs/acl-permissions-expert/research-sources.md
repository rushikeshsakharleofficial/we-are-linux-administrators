# Research sources

Primary references used:

- `acl(5)` for POSIX ACL concepts, entries, masks, and default ACL behavior.
- `getfacl(1)` for reading ACLs and output semantics.
- `setfacl(1)` for modifying, backing up, restoring, and removing ACLs.
- `chmod(1)` interaction with permission masks.
