# Notes on main branch state

During this implementation session, a few addon skill files were created directly on `main` before the branch strategy was recovered.

The addon PR branch also contains those files so the final review can happen through the branch diff where GitHub can compare what remains new against current `main`.

Future addon work should start from a fresh branch before any file creation.
