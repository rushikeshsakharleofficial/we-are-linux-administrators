# Validation

Run:

```bash
python3 tests/test_admin_addon_skill_pack.py
```

Expected output:

```text
admin addon skill pack tests passed
```

This validates that all initial addon skill directories exist and contain the required contract sections.
