# Research sources

Primary references used:

- `chmod(1)` for symbolic/octal modes and special-bit behavior.
- `chown(1)` for owner/group semantics and recursive/dereference behavior.
- `stat(1)` for inspection formats.
- POSIX/Linux filesystem permission behavior as exposed through `namei`, `findmnt`, and common GNU tools.
