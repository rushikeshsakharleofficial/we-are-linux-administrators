# Research sources

Primary references used for this module:

- GNU/coreutils and Linux man pages for `chmod`, `chown`, `stat`.
- Linux man pages for `find`, `xargs`, `grep`, `sed`, `awk`, and `bash`.
- POSIX shell principles where applicable.

The module intentionally focuses on method and safety, because command behavior varies by implementation, distro, shell, and tool version.
