# Cloudflare DNS/WAF/cache/rate-limit playbook

Cloudflare changes can affect DNS resolution, HTTP routing, origin TLS, cache behavior, security blocks, and customer traffic. Prefer one small change at a time. Validate DNS, HTTP status, TLS chain, headers, WAF events, cache status, and origin direct access.
