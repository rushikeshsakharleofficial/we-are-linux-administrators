# Cloudflare MCP/API workflow

Use Cloudflare MCP for read-only discovery where available. Export zone records, rulesets, SSL/TLS, cache, rate-limit and security settings. Use API/MCP changes only with least-privilege scopes, diff, validation, and rollback.
